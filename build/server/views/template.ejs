<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="keywords" content="">
    <meta name="description" content="">
    <title>
        <%= htmlWebpackPlugin.options.title || `欢迎使用无后端开发模式` %>
    </title>
    <script>
        window.pageConfig = {
        }
    </script>
    <style>
        body {
            font-size: 16px;
        }

        .t-r {
            text-align: center;
            color: #333;
            font-size: 16px;
            font-weight: normal;
            margin: 0 20px;
            padding: 10px 0;
            border-bottom: 1px dashed rgb(220, 220, 220);
        }
    </style>
</head>

<body>
    <div class="mod-app"></div>
    <% if (htmlWebpackPlugin.options.showHtmlWebpackPlugin) { %>
        <p>htmlWebpackPlugin信息:</p>
        <pre><b><%= JSON.stringify(htmlWebpackPlugin,null,4)%></pre>
        </p>
        <% } %>

</body>

</html>